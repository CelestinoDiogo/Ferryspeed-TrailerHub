import { ComingSoonPage } from "@/components/common/coming-soon-page";

export default function BoatCheckPage() {
  return (
    <ComingSoonPage
      title="Boat Check"
      description="Boat Check entrypoint navigation is being prepared. Existing vessel-level Boat Check workflows remain unchanged."
      backHref="/dashboard/vessel-operations"
    />
  );
}