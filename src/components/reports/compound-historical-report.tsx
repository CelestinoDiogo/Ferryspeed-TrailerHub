"use client";

import { useEffect, useMemo, useState } from "react";
import { HistoryDateRangeFilter } from "@/components/common/history-date-range-filter";
import { PrintFilters } from "@/components/print/print-filters";
import { PrintFooter } from "@/components/print/print-footer";
import { PrintHeader } from "@/components/print/print-header";
import { PrintReportLayout } from "@/components/print/print-report-layout";
import { PrintSummary } from "@/components/print/print-summary";
import { PrintTable } from "@/components/print/print-table";
import { createHistoryDateRange, getHistoryDateRangeLabel, normalizeHistoryPreset, type HistoryDateRangeValue } from "@/lib/history-date-range";
import { compoundActivityTypes, filterCompoundActivity, ownershipForCompoundTrailer, type CompoundActivityRecord, type CompoundReportMode, type CompoundSnapshotRecord } from "@/lib/reports/compound-historical";
import { supabase } from "@/lib/supabase";
import { getTrailerOwnershipBadgeLabel } from "@/lib/trailer-ownership";

const formatDateTime = (value?: string | null) => value ? new Date(value).toLocaleString("en-GB", { day: "2-digit", month: "short", year: "numeric", hour: "2-digit", minute: "2-digit" }) : "-";
const readRange = (): HistoryDateRangeValue => { if (typeof window === "undefined") return createHistoryDateRange("today"); const p = new URLSearchParams(window.location.search); const preset = normalizeHistoryPreset(p.get("history")); return preset === "custom" ? { preset, startDate: p.get("start") ?? "", endDate: p.get("end") ?? "" } : createHistoryDateRange(preset); };

export function CompoundHistoricalReport({ mode }: { mode: CompoundReportMode }) {
  const [range, setRange] = useState<HistoryDateRangeValue>(() => readRange());
  const [ownership, setOwnership] = useState<"all" | "company" | "outsourcing">("all");
  const [loadStatus, setLoadStatus] = useState("all");
  const [search, setSearch] = useState("");
  const [position, setPosition] = useState("all");
  const [eventType, setEventType] = useState("all");
  const [snapshot, setSnapshot] = useState<CompoundSnapshotRecord[]>([]);
  const [activity, setActivity] = useState<CompoundActivityRecord[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    const load = async () => {
      setLoading(true); setError(null);
      try {
        if (mode === "snapshot") {
          const { data, error: queryError } = await supabase.from("trailers").select("id, trailer_number, compound_position, load_status, customer, trailer_type, is_local, operational_status, notes, trailer_source, external_company").is("departure_date", null).neq("is_local", true).order("compound_position", { ascending: true });
          if (queryError) throw queryError;
          setSnapshot((data ?? []).map((row) => ({ id: row.id, trailerNumber: row.trailer_number, ownershipType: ownershipForCompoundTrailer(row), position: row.compound_position, loadStatus: row.load_status, customer: row.customer, trailerType: row.trailer_type ?? null, localLabel: row.is_local ? "Local" : "External", currentStatus: row.operational_status, notes: row.notes ?? null })));
        } else {
          const { data, error: queryError } = await supabase.from("trailer_activity_log").select("id, trailer_id, trailer_number, event_type, previous_compound_position, new_compound_position, previous_status, new_status, source_module, performed_by, event_description, created_at").in("event_type", [...compoundActivityTypes]).order("created_at", { ascending: false }).limit(1200);
          if (queryError) throw queryError;
          const activityRows = data ?? [];
          const trailerIds = Array.from(new Set(activityRows.map((row) => row.trailer_id).filter((value): value is string => Boolean(value))));
          const { data: trailerRows, error: trailerError } = trailerIds.length ? await supabase.from("trailers").select("id, trailer_number, trailer_source, external_company, is_local").in("id", trailerIds) : { data: [], error: null };
          if (trailerError) throw trailerError;
          const trailerMap = new Map((trailerRows ?? []).map((row) => [row.id, row]));
          setActivity(activityRows.map((row) => ({ id: row.id, occurredAt: row.created_at, trailerNumber: row.trailer_number, eventType: row.event_type, ownershipType: ownershipForCompoundTrailer(row.trailer_id ? trailerMap.get(row.trailer_id) ?? { trailer_number: row.trailer_number } : { trailer_number: row.trailer_number }), previousPosition: row.previous_compound_position, newPosition: row.new_compound_position, previousStatus: row.previous_status, newStatus: row.new_status, sourceModule: row.source_module, performedBy: row.performed_by, description: row.event_description })));
        }
      } catch (loadError) { setError(loadError instanceof Error ? loadError.message : "Unable to load Compound report."); } finally { setLoading(false); }
    };
    void load();
  }, [mode]);

  const filteredSnapshot = useMemo(() => snapshot.filter((row) => (ownership === "all" || row.ownershipType === ownership) && (loadStatus === "all" || row.loadStatus?.toLowerCase() === loadStatus) && (position === "all" || row.position?.toLowerCase().includes(position.toLowerCase())) && (!search || [row.trailerNumber, row.customer, row.position].filter(Boolean).join(" ").toLowerCase().includes(search.toLowerCase()))), [snapshot, ownership, loadStatus, position, search]);
  const filteredActivity = useMemo(() => filterCompoundActivity(activity, range, ownership, search, eventType), [activity, range, ownership, search, eventType]);
  const rows = mode === "snapshot" ? filteredSnapshot : filteredActivity;
  const printRows = rows as Array<CompoundSnapshotRecord & CompoundActivityRecord>;
  const title = mode === "snapshot" ? "COMPOUND SNAPSHOT REPORT" : "COMPOUND ACTIVITY REPORT";
  const printAt = new Date().toLocaleString("en-GB");
  const ownCount = rows.filter((row) => row.ownershipType === "company").length;
  const outsourcedCount = rows.filter((row) => row.ownershipType === "outsourcing").length;

  const updateRange = (next: HistoryDateRangeValue) => { setRange(next); const p = new URLSearchParams(window.location.search); p.set("history", next.preset); if (next.preset === "custom") { p.set("start", next.startDate); p.set("end", next.endDate); } else { p.delete("start"); p.delete("end"); } window.history.replaceState({}, "", `${window.location.pathname}?${p.toString()}`); };

  return <>
    <main className="screen-only min-h-screen bg-slate-950 px-4 py-6 text-slate-100 sm:px-6"><div className="mx-auto max-w-7xl space-y-5"><header className="rounded-3xl border border-white/10 bg-slate-900/80 p-5"><p className="text-xs font-semibold uppercase tracking-[0.25em] text-cyan-400">Compound Reports</p><h1 className="mt-2 text-3xl font-semibold">{title}</h1><p className="mt-2 text-sm text-slate-300">Read-only Compound {mode === "snapshot" ? "state snapshot" : "activity history"}.</p></header><section className="rounded-3xl border border-white/10 bg-slate-900/80 p-4"><div className="flex flex-wrap items-end gap-4">{mode === "activity" ? <HistoryDateRangeFilter value={range} onChange={updateRange} label="Period" /> : null}<label className="text-xs font-semibold uppercase tracking-[0.18em] text-slate-400">Ownership<select value={ownership} onChange={(e) => setOwnership(e.target.value as typeof ownership)} className="mt-2 block h-11 rounded-xl border border-white/10 bg-slate-950 px-3 text-sm normal-case tracking-normal text-white"><option value="all">All</option><option value="company">Own</option><option value="outsourcing">Outsourced</option></select></label>{mode === "snapshot" ? <><label className="text-xs font-semibold uppercase tracking-[0.18em] text-slate-400">Load Status<select value={loadStatus} onChange={(e) => setLoadStatus(e.target.value)} className="mt-2 block h-11 rounded-xl border border-white/10 bg-slate-950 px-3 text-sm normal-case tracking-normal text-white"><option value="all">All</option><option value="empty">Empty</option><option value="loaded">Loaded</option><option value="maintenance">Maintenance</option></select></label><label className="text-xs font-semibold uppercase tracking-[0.18em] text-slate-400">Position<input value={position === "all" ? "" : position} onChange={(e) => setPosition(e.target.value || "all")} placeholder="Any position" className="mt-2 block h-11 rounded-xl border border-white/10 bg-slate-950 px-3 text-sm normal-case tracking-normal text-white" /></label></> : <label className="text-xs font-semibold uppercase tracking-[0.18em] text-slate-400">Event<select value={eventType} onChange={(e) => setEventType(e.target.value)} className="mt-2 block h-11 rounded-xl border border-white/10 bg-slate-950 px-3 text-sm normal-case tracking-normal text-white"><option value="all">All Events</option>{compoundActivityTypes.map((type) => <option key={type} value={type}>{type.replaceAll("_", " ")}</option>)}</select></label>}<label className="text-xs font-semibold uppercase tracking-[0.18em] text-slate-400">Trailer search<input value={search} onChange={(e) => setSearch(e.target.value)} placeholder="Trailer or prefix" className="mt-2 block h-11 rounded-xl border border-white/10 bg-slate-950 px-3 text-sm normal-case tracking-normal text-white" /></label></div></section>{error ? <p className="rounded-xl border border-rose-300 bg-rose-950/40 p-3 text-sm text-rose-200">{error}</p> : null}{loading ? <p className="rounded-xl bg-slate-900 p-4 text-sm text-slate-300">Loading Compound report...</p> : null}{!loading && rows.length === 0 ? <p className="rounded-xl bg-slate-900 p-4 text-sm text-slate-300">No Compound records match the selected filters.</p> : null}{!loading && rows.length > 0 ? <><section className="grid gap-3 sm:grid-cols-3"><div className="rounded-2xl bg-slate-900 p-4"><p className="text-xs uppercase text-slate-400">Total {mode === "snapshot" ? "in Compound" : "Events"}</p><p className="mt-2 text-2xl font-bold">{rows.length}</p></div><div className="rounded-2xl bg-slate-900 p-4"><p className="text-xs uppercase text-slate-400">Own</p><p className="mt-2 text-2xl font-bold">{ownCount}</p></div><div className="rounded-2xl bg-slate-900 p-4"><p className="text-xs uppercase text-slate-400">Outsourced</p><p className="mt-2 text-2xl font-bold">{outsourcedCount}</p></div></section><div className="overflow-x-auto rounded-2xl border border-white/10 bg-slate-900"><table className="min-w-full text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase text-slate-400"><tr>{mode === "snapshot" ? <><th className="p-3">Trailer</th><th className="p-3">Ownership</th><th className="p-3">Position</th><th className="p-3">Load Status</th><th className="p-3">Customer</th><th className="p-3">Local / External</th><th className="p-3">Current Status</th></> : <><th className="p-3">Date / Time</th><th className="p-3">Trailer</th><th className="p-3">Event</th><th className="p-3">Ownership</th><th className="p-3">Previous Position</th><th className="p-3">New Position</th><th className="p-3">Source</th><th className="p-3">Description</th></>}</tr></thead><tbody>{rows.map((row) => mode === "snapshot" ? <tr key={row.id} className="border-b border-white/5"><td className="p-3 font-semibold">{row.trailerNumber ?? "-"}</td><td className="p-3">{getTrailerOwnershipBadgeLabel(row.ownershipType)}</td><td className="p-3">{row.position ?? "-"}</td><td className="p-3">{row.loadStatus ?? "-"}</td><td className="p-3">{row.customer ?? "-"}</td><td className="p-3">{row.localLabel}</td><td className="p-3">{row.currentStatus ?? "-"}</td></tr> : <tr key={row.id} className="border-b border-white/5"><td className="p-3">{formatDateTime(row.occurredAt)}</td><td className="p-3 font-semibold">{row.trailerNumber ?? "-"}</td><td className="p-3">{row.eventType}</td><td className="p-3">{getTrailerOwnershipBadgeLabel(row.ownershipType)}</td><td className="p-3">{row.previousPosition ?? "-"}</td><td className="p-3">{row.newPosition ?? "-"}</td><td className="p-3">{row.sourceModule ?? "-"}</td><td className="p-3">{row.description ?? "-"}</td></tr>)}</tbody></table></div></> : null}</div></main>
    <div className="print-only"><PrintReportLayout orientation="landscape"><PrintHeader title={title} printedAt={printAt} totalRecords={rows.length}><PrintFilters items={[{ label: "Mode", value: mode }, { label: "Period", value: mode === "activity" ? getHistoryDateRangeLabel(range) : "Current snapshot" }, { label: "Ownership", value: ownership }, { label: "Search", value: search || "All records" }]} /></PrintHeader><PrintSummary items={[{ label: mode === "snapshot" ? "Total in Compound" : "Total Events", value: rows.length }, { label: "Own", value: ownCount }, { label: "Outsourced", value: outsourcedCount }]} /><PrintTable rows={printRows} columns={mode === "snapshot" ? [{ key: "trailer", header: "Trailer", render: (row) => row.trailerNumber ?? "-" }, { key: "ownership", header: "Ownership", render: (row) => getTrailerOwnershipBadgeLabel(row.ownershipType) }, { key: "position", header: "Position", render: (row) => row.position ?? "-" }, { key: "load", header: "Load Status", render: (row) => row.loadStatus ?? "-" }, { key: "customer", header: "Customer", render: (row) => row.customer ?? "-" }, { key: "status", header: "Current Status", render: (row) => row.currentStatus ?? "-" }] : [{ key: "date", header: "Date / Time", render: (row) => formatDateTime(row.occurredAt) }, { key: "trailer", header: "Trailer", render: (row) => row.trailerNumber ?? "-" }, { key: "event", header: "Event", render: (row) => row.eventType }, { key: "ownership", header: "Ownership", render: (row) => getTrailerOwnershipBadgeLabel(row.ownershipType) }, { key: "previous", header: "Previous Position", render: (row) => row.previousPosition ?? "-" }, { key: "next", header: "New Position", render: (row) => row.newPosition ?? "-" }, { key: "source", header: "Source", render: (row) => row.sourceModule ?? "-" }, { key: "description", header: "Description", render: (row) => row.description ?? "-" }]} /><PrintFooter /></PrintReportLayout></div>
  </>;
}
